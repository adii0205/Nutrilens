node_modules/
dist/
build/
.cache/
.env*
!.env.example
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Python ML Backend
ml_backend/venv/
ml_backend/__pycache__/
ml_backend/models/__pycache__/
*.pyc

# Downloaded ML datasets, checkpoints, and generated embeddings
/.ml_artifacts/

